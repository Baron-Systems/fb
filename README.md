# fb — Frappe Manager Backup Dashboard

Production-grade backup dashboard for Frappe Manager (fm).

## Run (zero-config)

After installing with `pipx` from GitHub:

- `fb run`

On first run it auto-creates state directories, SQLite DB, scheduler, and starts the web UI.


